import { Direcive1Directive } from './direcive1.directive';

describe('Direcive1Directive', () => {
  it('should create an instance', () => {
    const directive = new Direcive1Directive();
    expect(directive).toBeTruthy();
  });
});
